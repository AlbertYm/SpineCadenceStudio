# 仓库工作约定

先阅读 README.md 和 CONTRIBUTING.md。用户说明默认中文，代码/API 名称保持原文。

- 入口为 src/SpineCadenceStudio.cs，构建为 scripts/build.ps1。
- 按当前任务授权执行；源码编辑不自动授权 push、发布、可见性修改或许可变更。
- 修改前检查 git status，保护非本次产生的改动。
- 不读写 Spine 原始素材以外的无关目录，不覆盖原工程，不提交用户样本。
- 保持单个/批量、两种输出位置、原文件名保留、重名避让和失败明确提示。
- 新通道需要对应 Spine 版本语义证据和受影响路径验证；不能通过关闭验证来绕过失败。
- 不将构建成功当作真实 Spine 转换或 GUI 验收成功。
- 未获授权不维护全局配置、不安装软件、不创建子代理、不更改许可证。
