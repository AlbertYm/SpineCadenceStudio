# 修改、验证和上传规则

## 修改范围

- 修改前执行 `git status --short`，保护已有改动。一次提交围绕一个明确问题，不混入无关重构。
- 原始素材只读；输出新建目录、重名自动避让，工程和 JSON 保留原文件名。
- 总时长保持不变；事件时间保留；不支持的通道必须明确失败，不能静默忽略或假报成功。
- Spine 数据语义以对应版本官方格式和运行库为依据；新增通道同时检查初始值、插值、首尾时间和回读精度。

## 验证

1. 运行 `scripts/build.ps1`，确认编译成功。
2. 算法修改验证线性、保持式、曲线、非整帧结束时间和非法输入；使用有权限的代表性素材执行真实导出、导入和回读。
3. 输出修改验证原位置、指定目录、重复运行、中文及空格路径、文件名和原文件哈希。
4. UI 修改检查受影响入口、错误状态和缩放。编译、绘制检查、真实 GUI 操作、目标机验收分别记录，不能互相替代。
5. 文档修改检查链接与描述即可，不必重跑全部动画测试。

测试素材保持在仓库外。报告使用脱敏摘要，不上传原素材、导出数据、截图中的私人信息或完整本机日志。

## 提交与上传

- 上传前检查 `git diff --check`、`git diff --cached --stat` 和 `git diff --cached`，明确确认每个文件的用途。
- 禁止提交凭据、API key、Cookie、许可证、账号数据、用户素材、绝对私人路径、临时文件与构建目录。`.gitignore` 只是辅助，不能代替检查。
- 普通修改建议使用分支和 PR；有维护者明确授权时可以直接推送目标分支。禁止未经授权 force push、重写远端历史或删除分支/版本。
- 提交说明写清问题、修改和验证。失败的构建或测试不能表述为通过。
- 使用现有 GitHub 登录，不把凭据写入 remote URL、脚本或文档。网络失败时先核对远端是否已收到提交，避免重复操作。
- 上传后核对远端 commit SHA 和文件清单；本地 commit 成功不等于远端上传成功。

## 发布与回退

发布时同步 `Engine.Version`、程序集版本和文档；构建脚本从 Engine.Version 读取输出目录版本。
新版本独立打包，保留旧版本，提供 SHA-256、环境依赖、已验证范围和限制。EXE/ZIP 作为经审核的发布资产，不放进源码提交。
修改功能后只重测受影响路径；首次正式发布执行完整基线。发布资产、公开可见性或许可变更应由维护者明确授权。
回退代码使用可审查的 revert 提交；恢复动画使用原工程，不覆盖用户输出。
