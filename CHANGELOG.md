# 更新记录

以下 1.1.0–1.1.3 为 2026-09-21 的本地迭代，本次统一同步源码到 GitHub；版本号不代表此前已创建对应 Git 标签或 GitHub Release。

## 1.1.3 — 贝塞尔控制点兼容

- 修复部分可由 Spine 读取的工程因“曲线时间控制点顺序异常”被拒绝的问题。
- 骨骼与颜色通道允许时间控制点交叉或越出相邻关键帧区间，沿用 Spine 4.1 分段求值规则。继续检查控制点格式、非有限数值和关键帧时间顺序。
- 验证：真实故障样本的 55 个贝塞尔采样分量与官方运行库对照，最大绝对差约 0.0000243（阈值 0.0001）；真实转换、Spine 回读、二进制和图集导出通过，时长 1.8667 秒不变。
- 中文及空格路径下解压后的程序再次真实转换通过。上述是本地验证，不是仓库 CI；用户素材未上传。

## 1.1.2 — 独立游戏资源输出目录

- 新增游戏资源总目录选择，每个工程自动建立原名子文件夹，仅放 skel、atlas 和图集 PNG。
- 不指定目录时，资源放入工程结果内的“游戏资源”子目录，避免与可编辑工程混放。
- 重名目录追加序号，内部文件名不变；复制全部图集 PNG，支持多页输出。资源路径写入转换记录。
- 验证：两个真实工程的独立目录输出、同名重复输出、文件类型隔离，以及中文解压路径下的默认输出通过。
- 新入口做过界面绘制检查，不等同于真实高 DPI 或完整桌面交互验收。

## 1.1.1 — 附件网格变形

- 新增 Spine 4.1 `attachments/deform` 采样，支持顶点 offset、零值省略、线性、保持式和贝塞尔过渡。
- 回读时按展开后的顶点偏移比较，避免相同数据的不同稀疏存储形式导致误报。
- 增加变形采样规模上限；序列附件等未知通道仍明确拒绝，不静默跳过。
- 验证：真实样本 6 条变形通道、20,382 个顶点采样值独立核对通过；时长 3.8667 秒不变；工程回读与资源导出通过。
- 真实样本覆盖线性及保持式变形，未单独完成真实贝塞尔变形素材的观感验收。

## 1.1.0 — 降帧后导出游戏资源

- 新增可选的一键导出 skel、atlas、PNG 和自定义打包预设入口。
- 最终实现分别调用 binary 导出及图集 pack，随程序提供两个设置 JSON；不能省略这些配置文件。
- 默认预设按需求配置为最大 1024、正方形、4 的倍数、非 2 的幂、零间距、不预乘 Alpha、Linear 过滤。
- 导出在临时目录完成并检查产物后才发布，避免提前占用最终目录导致保存失败。
- 验证：真实样本降帧、回读及三类资源生成通过；尚未宣称目标游戏运行时导入已验收。

## 1.0.x — 初始功能历史

- 1.0.3：动态插槽颜色和透明度，支持 rgba、rgb、alpha、rgba2、rgb2。
- 1.0.2：保留输出工程和 JSON 的原文件名，仅结果目录添加帧率和避让后缀。
- 1.0.1：骨骼贝塞尔采样；失败状态改为红色，重新执行清理旧日志。
- 1.0.0：Windows 图形界面、单个和批量降帧、原位置或指定目录输出、独立新工程及原文件哈希保护。

## 当前限制和验证边界

- 依赖 Windows x64、.NET Framework 4.8 和已激活的 Spine 4.1.24 Professional。
- 多骨架、约束动画、序列附件、外部音频等仍不支持；JSON 往返不保证保留全部编辑器专属数据。
- 未完成另一台机器、真实高 DPI、全部复杂素材的编辑器观感或目标游戏运行时验收。新版本不自动搬移旧结果。
- 源码、文档和预设可从仓库构建；用户素材、导出数据、私人路径及本机日志不入库。
